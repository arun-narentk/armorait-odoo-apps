<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_whatsapp_reminders" model="ir.cron">
        <field name="name">WhatsApp: Create follow-up and payment reminders</field>
        <field name="model_id" ref="account_invoice_whatsapp.model_whatsapp_reminder"/>
        <field name="state">code</field>
        <field eval="False" name="active" />
        <field name="code">model._cron_whatsapp_reminders()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
    </record>
</odoo>
